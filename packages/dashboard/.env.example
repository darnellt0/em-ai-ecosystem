# API Base URL
# For Docker: Use the host's localhost since API is mapped to host port 3000
# For Local Development: Use http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Dashboard Port
# Docker: Runs on port 3001 (mapped to host 3001)
# Local: Default is 3000, use 3001 to avoid conflicts with API
PORT=3001

# Node Environment
NODE_ENV=production
